import { NtscurrencyPipe } from './ntscurrency.pipe';

describe('NtscurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new NtscurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
